import React from 'react';
import Toast from 'react-native-toast-message';

const toastConfig = {
    // Customize your toast here if needed
};

const ToastConfig = () => {
    return <Toast config={toastConfig} />;
};

export default ToastConfig;
